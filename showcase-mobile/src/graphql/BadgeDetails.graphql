query BadgeDetails($id: String!) {
  badgeType(where: { id: $id }) {
    id
    donationAmount
    isOwnedByMe
    isCreatedByMe
    publicUrl
    viewCount
    likeCount
    creator {
      id
      profile {
        id
        displayName
      }
    }
    cause {
      id
      imageUrl
      name
      site
    }
    ...BuyBadgeCriterias
  }
}
