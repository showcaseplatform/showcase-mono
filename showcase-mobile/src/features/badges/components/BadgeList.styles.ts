import { FlatList } from 'react-native'
import styled from 'styled-components/native'

// temp. out of use, TS does not like it :(

export const BadgeList = styled(FlatList).attrs({
  contentContainerStyle: {
    paddingHorizontal: 4,
  },
})``
